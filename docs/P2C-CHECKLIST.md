# P2c 人工验收清单

记录实例、构建版本和操作者：____________________

| 项目 | 步骤 | 通过判据 | 结果 / 证据 | 日期 |
| --- | --- | --- | --- | --- |
| 多 DPI | Windows 显示设置依次切换 100% → 150% → 200% → 100%，每档往返面板一次。 | 文字、命中区域和布局正确，无裁切。 | ____ | ____ |
| Explorer 重启 | 在任务管理器重启 Windows Explorer，共 3 次。 | 无重复面板窗口；图标恢复正常。 | ____ | ____ |
| Win+D | 按 Win+D 隐藏和恢复桌面，共 3 次。 | 面板可恢复显示，无残留。 | ____ | ____ |
| 断线重连 | 保持面板运行，执行 `scripts/lab/p2c-daemon-restart.ps1 -Name <实例名>`。 | 脚本 PASS，记录新 `subscribe` 和 `snapshot.received` 日志。 | ____ | ____ |
| 双实例视觉 | 同时打开 lab-a r42 与 lab-b r43，核对各自面板数字与 fixture。 | 两组数据同时显示正确数字，无交叉。 | ____ | ____ |
| 配置损坏 UX | 备份实例 config，把实例 config 替换为截断 JSON 后重启应用；验收后恢复备份。 | 启动日志有 `config.load_issue` 警告；原文件被隔离为 `config.corrupt-*`；应用仍可用。 | ____ | ____ |
